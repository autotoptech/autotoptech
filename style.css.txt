/* Set a clean, readable font like the example site */
body {
    font-family: monospace, Courier New, Courier, sans-serif;
    line-height: 1.6;
    color: #333;
    background-color: #f9f9f9;
    margin: 0;
    padding: 0;
}

/* Center the main content area */
.container {
    width: 90%;
    max-width: 700px; /* Limits the content width */
    margin: 40px auto; /* Centers the container on the page */
    padding: 20px;
}

/* Style the main headings */
h1, h2 {
    text-align: center;
    text-transform: uppercase;
    letter-spacing: 2px;
}

/* Style the horizontal line separators */
hr {
    border: 0;
    height: 1px;
    background-color: #ccc;
    margin: 30px 0;
}

/* Style the project list */
ul {
    list-style: none; /* Removes bullet points */
    padding: 0;
}

li {
    margin-bottom: 20px;
}

li a {
    font-weight: bold;
    text-decoration: none; /* Removes the underline */
    color: #007bff; /* A simple blue color */
}

li a:hover {
    text-decoration: underline;
}

.date {
    float: right; /* Puts the date on the right */
    color: #777;
    font-size: 0.9em;
}

.description {
    margin-top: 5px;
    font-size: 0.9em;
}

